# PEP8 Guidelines as written by Guido van Rossum, the creator of Python.

# 1. limit all lines to a maximum of 79 characters.
#2. Linebreak should occur before a binary operator, not after.
income = (gross_wages
            + taxable_interest)
# 3. Imports should be at the top of a file.
import json
import requests

# 4. Never use characters I, O, or l as single character variable names.
# 5. Always use spaces unless you are trying to be consistent with code that already uses tabs..